import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:unitodo/app/modules/add/add_module.dart';

void main() {
  initModule(AddModule());
  // AddController add;
  //
  setUp(() {
    //     add = AddModule.to.get<AddController>();
  });

  group('AddController Test', () {
    //   test("First Test", () {
    //     expect(add, isInstanceOf<AddController>());
    //   });

    //   test("Set Value", () {
    //     expect(add.value, equals(0));
    //     add.increment();
    //     expect(add.value, equals(1));
    //   });
  });
}
